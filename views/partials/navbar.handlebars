
<nav class="navbar navbar-expand-lg navbar-light bg-dark">

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse">
    <ul class="navbar-nav mr-auto">
      {{#if currentUser}}
        <li class="nav-item active">
          <a href="/">Zuccbook</a>
        </li>
      {{else}}
        <li class="nav-item active">
          <a href="/feed">Zuccbook</a>
        </li>
      {{/if}}
    </ul>
    
    <ul class="nav-item active navbar-nav navbar-right">
      {{#if currentUser}}
        <li><a href="/posts/new" class="btn btn-primary navbar-btn">New Post</a></li>
        <li><a href="/logout" class="btn btn-primary navbar-btn">Logout</a></li>
      {{else}}
        <li><a href="/" class="btn btn-primary navbar-btn">Login</a></li>
        <li><a href="/sign-up" class="btn btn-primary navbar-btn">Sign Up</a></li>
      {{/if}}
    </ul>
    
  </div>
</nav>